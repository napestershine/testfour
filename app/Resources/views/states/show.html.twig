{% extends 'base.html.twig' %}

{% block body %}
    <h1>States</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ state.id }}</td>
            </tr>
            <tr>
                <th>State</th>
                <td>{{ state.state }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('states_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('states_edit', { 'id': state.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
