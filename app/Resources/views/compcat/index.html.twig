{% extends 'base.html.twig' %}

{% block body %}
    <h1>CompCat list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for compCat in compCats %}
            <tr>
                <td><a href="{{ path('compcat_show', { 'id': compCat.id }) }}">{{ compCat.id }}</a></td>
                <td>{{ compCat.name }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('compcat_show', { 'id': compCat.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('compcat_edit', { 'id': compCat.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('compcat_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
