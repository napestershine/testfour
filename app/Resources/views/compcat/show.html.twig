{% extends 'base.html.twig' %}

{% block body %}
    <h1>CompCat</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ compCat.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ compCat.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('compcat_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('compcat_edit', { 'id': compCat.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
