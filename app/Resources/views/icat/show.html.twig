{% extends 'base.html.twig' %}

{% block body %}
    <h1>Icat</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ icat.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ icat.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('icat_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('icat_edit', { 'id': icat.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
